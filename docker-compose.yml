services:
  telegram-bot:
    build: .
    container_name: telegram-bot-pem
    restart: always  # Restart if the bot crashes
    env_file:
      - .env 
    environment:
    - TOKEN=${{ secrets.TELEGRAM_BOT_TOKEN }}